import styles from "./mainFooter.module.css";

const MainFooter = () => {
  return (
    <footer className={styles.container}>
      <p>จัดทำโดย Project_Man_dev</p>
    </footer>
  );
};

export default MainFooter;
