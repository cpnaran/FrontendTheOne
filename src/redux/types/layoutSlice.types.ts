export interface LayoutState {
  header: boolean;
  main: boolean;
  footer: boolean;
  sidebar:boolean;

}

export interface SetLayoutState {
  header: boolean;
  main: boolean;
  footer: boolean;
  sidebar:boolean;
}
